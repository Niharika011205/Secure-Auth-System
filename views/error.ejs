

<div class="container">
    <div style="text-align: center; padding: 4rem 0;">
        <div class="card" style="max-width: 600px; margin: 0 auto;">
            <div style="color: #e74c3c; margin-bottom: 2rem;">
                <i class="fas fa-exclamation-circle" style="font-size: 4rem;"></i>
            </div>
            
            <h1 style="color: #333; margin-bottom: 1rem;">Server Error</h1>
            <h2 style="color: #e74c3c; margin-bottom: 1rem;">Something went wrong</h2>
            
            <p style="color: #666; margin-bottom: 2rem; font-size: 1.1rem;">
                We're experiencing some technical difficulties. Please try again later.
            </p>
            
            <% if (typeof error !== 'undefined' && error.stack) { %>
                <div style="background: #f8f9fa; padding: 1rem; border-radius: 8px; margin: 1rem 0; text-align: left;">
                    <h4 style="color: #e74c3c; margin-bottom: 0.5rem;">Error Details:</h4>
                    <pre style="color: #666; font-size: 0.8rem; overflow-x: auto;"><%= error.stack %></pre>
                </div>
            <% } %>
            
            <div>
                <a href="/" class="btn btn-primary">
                    <i class="fas fa-home"></i> Go Home
                </a>
                <button onclick="window.history.back()" class="btn btn-outline" style="margin-left: 1rem;">
                    <i class="fas fa-arrow-left"></i> Go Back
                </button>
            </div>
        </div>
    </div>
</div>